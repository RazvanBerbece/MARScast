# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'MARScast' do
  # Comment the next line if you don't want to use dynamic frameworks
  pod 'SwiftyJSON', '~> 4.0'
  pod 'Alamofire', '~> 5.0.0-rc.3'
  pod 'SwiftyGif'
  use_frameworks!

  # Pods for MARScast

  target 'MARScastTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'MARScastUITests' do
    # Pods for testing
  end

end
